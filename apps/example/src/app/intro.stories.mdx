<!-- Badge.stories.mdx -->

import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { moduleMetadata } from '@storybook/angular';

import { CommonModule } from '@angular/common';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { MetadFormlyModule } from './formly.module'

import { MetaFormlyComponent } from './formly.component';

<Meta title="Intro" component={MetaFormlyComponent} decorators={[moduleMetadata({imports: [BrowserAnimationsModule, MetadFormlyModule]})]}/>

# Metad Formly Components 👋

Let's define a story for our `Formly` components

## Material Components

### Textarea for JSON Type

```javascript
{
  key: 'value',
  type: 'json',
  templateOptions: {
    label: 'JSON'
  }
}
```

<Story id="material-json--primary"></Story>

You can set attributes in templateOptions to change material component appearance:

```javascript
[
  {
    key: "value",
    type: "json",
    templateOptions: {
      label: "JSON Outline",
      attributes: {
        appearance: "outline",
      },
    },
  },
  {
    key: "value2",
    type: "json",
    templateOptions: {
      label: "JSON Fill",
      attributes: {
        appearance: "fill",
      },
    },
  },
]
```

Preview:

<Story id="material-json--appearance"></Story>

### Array

<Story id="material-array--primary"></Story>

### Material Tabs

```javascript
{
  key: 'value',
  type: 'tabs',
  fieldGroup: [
    {
      key: 'xAxis',
      templateOptions: {
        label: 'X Axis'
      },
      fieldGroup: [
        {
          key: 'show',
          type: 'toggle',
          templateOptions: {
            label: 'Is Show'
          }
        },
        {
          key: 'type',
          type: 'select',
          templateOptions: {
            label: 'Type',
            options: [
              {value: 'value', label: 'Value'},
              {value: 'category', label: 'Category'}
            ]
          }
        }
      ]
    },
    {
      key: 'yAxis',
      templateOptions: {
        label: 'Y Axis'
      },
      fieldGroup: [...]
    }
  ],
  templateOptions: {
    label: 'Tabs Group'
  }
}
```

<Story id="material-tabs--primary"></Story>

### Material Color Picker

Use type `color` for color picker field.

The schema:

```javascript
{
  key: 'value',
  type: 'color',
  templateOptions: {
    label: 'Color'
  }
}
```

Preview:

<Story id="material-color-picker--primary"></Story>

### Code Editor

<Story id="material-code-editor--primary" />

## Ant Design Components

### Textarea

<Story id="antd-textarea--primary"></Story>
